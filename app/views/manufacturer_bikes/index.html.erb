<h1><%=@manufacturer.name%></h1>
<h3> <%= button_to "Create Bike", "/manufacturers/#{@manufacturer.id}/bikes/new", method: :get %>
<h3><%= link_to "Sort Bikes Alphabetically", sort: :alphabetically %></h4>
<%= form_with url: "/manufacturers/#{@manufacturer.id}/bikes", method: :get, local: true do |form| %>
  <%= form.label :Minimum_wheel_size %>
  <%= form.text_field :Minimum_wheel_size %>
  <%= form.submit "Filter Wheel Size" %>
  <% end %>
<% @bikes.each do |bike| %>
  <h3><%= link_to bike.name, "/bikes/#{bike.id}" %> </h3>
  <p> <%= link_to "Update #{bike.name}", "/bikes/#{bike.id}/edit" %> | <%= link_to "Delete #{bike.name}", "/manufacturers/#{@manufacturer.id}/bikes/#{bike.id}", method: :delete %> </p>
  <p>Wheel Size: <%= bike.wheelsize %></p>
  <p>Carbon: <%= bike.carbon %></p>
<% end %>